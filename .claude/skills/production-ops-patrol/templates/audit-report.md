# 🏥 Production Patrol Report

**Patrol ID:** `{patrol_id}`
**Time:** `{timestamp}` (UTC)
**Window:** `{window_minutes}` minutes
**Overall Result:** `{overall_result}`

---

## Status Overview

| Dimension | Status | Details |
|-----------|--------|---------|
| A. Docker Status | {A_icon} | {A_detail} |
| B. Anomaly Logs | {B_icon} | {B_detail} |
| C. Tracebacks | {C_icon} | {C_detail} |
| D. HTTP Health | {D_icon} | {D_detail} |
| E. Database | {E_icon} | {E_detail} |
| F. Resources | {F_icon} | {F_detail} |
| G. Revision | {G_icon} | {G_detail} |

---

## Anomalies Detected

{anomalies_section}

<!-- For each anomaly:
### {title}
- **Classification:** {classification}
- **Fingerprint:** `{fingerprint}`
- **Container:** {container}
- **First Seen:** {first_seen}
- **Occurrences:** {occurrences}
- **Description:** {description}
- **Traceback:**
  ```
  {traceback_excerpt}
  ```
-->

---

## Actions Taken

{actions_section}

<!-- For each action:
- **{action}** on `{container}` — {reason}
  - Time: {timestamp}
  - Result: {success/failure}
-->

---

## GitHub Issues

{github_section}

<!-- For each linked issue:
- #{issue_number} [{issue_title}]({issue_url}) — fingerprint `{fingerprint}`
-->

---

## Revision Info

| Service | Image |
|---------|-------|
| Backend | `{backend_image}` |
| Agent | `{agent_image}` |
| Scheduler Worker | `{scheduler_worker_image}` |

---

## Resource Snapshot

| Container | CPU | Memory | Restarts (1h) | OOM |
|-----------|-----|--------|---------------|-----|
| {container} | {cpu} | {memory} | {restarts} | {oom} |

---

## Next Steps

{next_steps}

<!-- Examples:
- ✅ No action needed — system is healthy
- 📋 Review GitHub issue #{number} for code defect fix
- 🔧 Investigate {anomaly} manually
- ⏳ Cooldown active for {container}, next restart eligible at {time}
-->

---

_Report generated by production-ops-patrol_
_Audit log: `~/.hermes/state/production-ops-patrol.db`_
