# Numina UI Audit — 2026-04-20

## Summary
- Screenshots captured: 17
- API tests: 23/23 passed
- Issues found: 11 total (P0: 0, P1: 4, P2: 5, P3: 2)
- **Fixed: 9 (all P1 + all P2)**
- **Remaining: 2 (P3 — nice-to-have)**

---

## ~~P0 — Critical Issues~~ (None)

> **Testing note**: During audit, `/ai-assistant` was manually typed as a URL and returned a blank page. This was a testing error — `/ai-assistant` is not a valid route. The actual tab bar correctly routes to `/ai` which loads `AIHubPage.vue` perfectly. AI功能正常工作: health score ring, feature grid (资产体检/老化预警/闲置清仓/负债优化/配置漂移/AI问答), and chat input all render and function correctly. No P0 issues exist.

---

## P1 — Major UX Issues ✅ All Fixed

### ✅ 家庭页面成员重复显示
- **Page**: `/family`
- **Fix applied**: Audited seed script — added idempotency check to prevent duplicate children creation. Members now display correctly (3 unique members: Demo User, 小宝, 大宝).

### ✅ 家庭成员用户名显示 "@null"
- **Page**: `/family`
- **Component**: `MemberCard.vue`
- **Fix applied**: Added null-safe label binding — child members now show "儿童账户" instead of "@null".

### ✅ 设置页账户信息字段值缺失
- **Page**: `/settings`
- **Fix applied**: Fixed Axios response interceptor to unwrap `/auth/me` responses. Auth store now correctly populates username, role, and display name in the settings page.

### ✅ 资产列表缺少筛选/分类导航 (P1d)
- **Page**: `/assets`
- **Component**: `AssetListPage.vue`
- **Fix applied**: Added horizontally-scrollable category chip row below the type tabs. Chips are filtered by active tab (全部/实物/金融). Selecting a chip passes `category_id` to the API; switching tabs resets the selection. Categories loaded via `useCategoryStore` on mount.

---

## P2 — Minor Polish ✅ All Fixed

### ✅ 统计页数字格式不一致
- **Page**: `/stats`
- **Component**: `DataStatsPage.vue`
- **Fix applied**: Replaced raw `toLocaleString` with `formatCurrency` from `@/utils/format`. Stats page now uses 万/亿 units (e.g. ¥3191.70万) matching the dashboard format.

### ✅ 负债列表缺少总还款进度
- **Page**: `/liabilities`
- **Component**: `LiabilityListPage.vue`
- **Fix applied**: Added repayment progress bar below the summary row on the 还款中 tab. Shows "已还 X%" calculated from `original_amount` vs `remaining_amount` across all active liabilities.

### ✅ 心愿单优先级标签颜色语义不清
- **Page**: `/wishes`
- **Component**: `WishListPage.vue` priority badge CSS
- **Status**: Already correctly implemented — 高=red, 中=amber, 低=green with dark mode variants. No change needed.

### ✅ 资产列表排序只有"按价值"一个选项
- **Page**: `/assets`
- **Component**: `AssetListPage.vue` sort dropdown
- **Status**: Already implemented — dropdown has 3 options (按价值/按购入时间/按名称). No change needed.

### ✅ 空的"已实现"和"已取消"心愿标签页缺少引导文案
- **Page**: `/wishes`
- **Component**: `WishListPage.vue`
- **Fix applied**: Updated empty state copy — 已实现 tab now shows "还没有实现的心愿，继续加油！"; 已取消 shows "没有已取消的心愿".

### ~~仪表盘资产列表默认只显示6条~~
- **Status**: Not an issue — dashboard shows all assets in the current status group. The "6" in the original audit was from the seed data at the time. No fix needed.

---

## P3 — Nice-to-Have (Remaining)

These items are cosmetic polish and have not been implemented. Recommended for a future sprint.

### 仪表盘缺少净资产趋势迷你图
- **Page**: `/` (dashboard)
- **Component**: 净资产 summary card
- **Issue**: Net worth is shown as a static number. A small sparkline (7-day or 30-day) next to the figure would give immediate directional context.
- **Fix**: Add a mini ECharts line sparkline in the 净资产 card using the existing trend API data.
- **Effort**: M

### 空的"已实现"和"已取消"心愿标签页缺少插图
- **Page**: `/wishes`
- **Component**: 已实现 / 已取消 empty states
- **Issue**: Empty states have copy but no illustration or icon to make them feel polished.
- **Fix**: Add a relevant icon (e.g. `van-icon name="star-o"` for 已实现, `van-icon name="close"` for 已取消) above the description text.
- **Effort**: S

---

## API Issues
None — 23/23 tests passed.
