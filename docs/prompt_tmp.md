```
对 ‘@books/Effective Java, Third Edition (Joshua Bloch) (z-library.sk, 1lib.sk, z-lib.sk).mobi' 调用改进后的skill进行处理直至生成最终版的epub，处理完后，逐页核对最终的epub与mobi转换后的原始epub，检查翻译后的内容是否与原文（英文）语义一致、技术术语恰当保留、章节排版结构合理、对代码和图片添加的描述恰当。

你需要调用ralph skill多次执行如上的`调用组合skill生成终版epub->与mobi转换后的原始epub版本比对->发现问题->按照skill的不同职责定位以及组合关系找到问题所在->优化skill->调用组合skill生成终版epub`，通过不超过100次的循环，直至生成的文件彻底满足我们的要求


```



```
你现在要作为一个“资深产品经理 + 资深 UI/UX 设计师 + 资深全栈工程师（Python backend / Vue + TypeScript H5 frontend）”来工作。

请先扫描整个工程目录，定位资产/总览相关前后端代码，再进入截图分析和方案设计。

你的目标不是简单模仿截图，而是基于参考 App 的截图，深度还原其产品逻辑、信息架构、状态设计、列表组织方式、卡片结构、总览逻辑和展示策略，并将这些能力系统性迁移到当前项目中，对“资产 / 总览”相关模块进行重构，使当前项目在交互和 UI 上尽可能与参考 App 一致。

## 强制执行顺序

1. 必须先调用 superpower 的 brainstorm 能力。
2. 对比分析：
   - docs/images/reference
   - docs/images/current
3. 必须先理解当前工程代码结构，再决定改造方案。
4. 先输出分析结论、差距分析、PRD 和技术改造方案，再开始编码。
5. 每完成一轮后，必须调用 oh-my-claude-code 的 ralph 技能做复盘。
6. 只要相对于 reference 仍有明显差距，就继续迭代，不允许“差不多就结束”。

## 任务目标

你需要完成以下事情：

### 1. 深度分析 reference 截图
对 reference 中每张图都不能只看表面 UI，而要深入分析：

- 页面目标是什么
- 用户进入页面后最先获取什么信息
- 信息架构如何组织
- 总览区、状态区、列表区之间是什么关系
- 是否存在聚合统计、状态切换、筛选、分类、卡片点击、详情跳转等逻辑
- 列表为什么是卡片式而不是普通列表
- 每张卡片承载了哪些主信息 / 副信息 / 状态信息
- 为什么需要图片，图片对识别和体验的价值是什么
- 背后隐含了怎样的数据结构和状态模型

例如看到“总资产统计”“服役中”“已退役”“资产卡片”“资产图片”时，你必须推导出：
- 系统存在资产生命周期状态管理
- 首页承担全局总览职责
- 状态既用于展示，也用于聚合、筛选、分流
- 列表是产品化资产管理，不是简单数据罗列
- 资产展示应尽量支持官方图片 / 品牌图 / 缩略图等更优体验

### 2. 对比 current，找出系统性差距
你必须明确指出当前项目在以下方面与 reference 的差距：

- 总览结构
- 状态管理
- 资产聚合能力
- 列表组织方式
- 卡片化程度
- 图片展示能力
- 筛选/分类能力
- 页面层级和阅读路径
- 交互流畅度
- UI 一致性
- H5 体验质量

并标明这些问题分别属于：
- 产品逻辑问题
- 交互设计问题
- UI 问题
- 前端实现问题
- 后端数据支持问题

## 重要约束：必须适配当前工程

当前工程真实技术栈是：

- 后端：Python
- 前端：Vue + TypeScript
- 端形态：H5

所以你必须严格按这个工程现实来做，避免实现跑偏。

### 后端侧必须重点关注
先理解并基于真实代码判断：
- 资产相关数据模型
- 总览/统计接口
- 状态字段
- 分类字段
- 图片字段
- 聚合逻辑
- 筛选/排序能力

必要时补齐：
- 状态管理字段
- 总览聚合接口
- 列表所需展示字段
- 图片资源字段或映射逻辑
- 分类/筛选支持能力

### 前端侧必须重点关注
先理解并基于真实代码判断：
- Vue 页面结构
- 路由结构
- 资产/总览相关页面
- 组件拆分
- TypeScript 类型定义
- API 调用封装
- 状态管理方式
- 当前 H5 交互方式

必要时重构：
- 首页/总览页结构
- 资产列表区
- 资产卡片组件
- 状态切换组件
- 分类/筛选交互
- 图片展示与兜底
- 加载态/空态/错误态
- H5 点击热区、滚动体验和首屏信息密度

## 不允许出现的错误

- 不要输出 React / Next.js / Flutter / 原生 iOS / Android 方案，除非工程真实存在
- 不要只改样式，不改产品逻辑和数据结构
- 不要把本应后端提供的数据能力硬塞给前端临时拼装
- 不要无视 H5 场景直接照搬原生 App 交互
- 不要想当然重写整个工程，优先在现有代码结构内做可落地重构
- 不要只说建议，必须真正完成代码修改

## 输出要求

在编码前，先输出以下内容：

### A. reference 深度拆解
逐图分析：
- 页面目标
- 信息架构
- 模块结构
- 交互逻辑
- 状态模型
- 列表/卡片逻辑
- 数据需求
- 图片策略

### B. 当前项目差距分析
按模块列出：
- 现状
- 与 reference 的差距
- 问题归属（产品 / UI / 前端 / 后端）
- 改造优先级

### C. 重构 PRD
至少包含：
- 改造目标
- 页面结构重构
- 总览设计
- 状态体系设计
- 资产列表和卡片设计
- 图片展示方案
- H5 交互说明
- 验收标准

### D. 技术改造方案
分别说明：
- Python 后端要改什么
- Vue + TypeScript 前端要改什么
- 需要新增/调整哪些字段、接口、类型、组件
- 如何分阶段落地

## 编码要求

开始实现后，你必须：
1. 先列出改动清单；
2. 按合理顺序推进改造；
3. 保持代码风格与现有工程一致；
4. 避免引入不必要的新依赖；
5. 确保代码可运行，不要只生成草稿；
6. 每完成一轮，都重新对照 reference 检查完成度。

## 强制循环复盘机制

每当你觉得自己做完了，必须立即调用 ralph，从“资深产品经理 + 资深 UI 设计师”的双重视角继续审查：

- 产品逻辑是否真的对齐了 reference
- 首页是否真正形成“总览 -> 状态 -> 列表”的认知路径
- 状态体系是否完整
- 列表是否已经具备卡片化、可识别、可管理能力
- 图片能力是否足够支撑更好的展示体验
- H5 交互是否自然
- UI 是否仍有明显粗糙、不一致、不高级的地方
- 前后端是否仍然存在割裂

每轮复盘都必须输出：
1. 当前完成度评估
2. 相比 reference 的剩余差距
3. 下一轮继续改进项
4. PRD / 设计 / 技术补充

只要还存在明显差距，就继续迭代，直到你确认：
- reference 关键截图已被充分理解
- 当前项目的资产/总览模块已完成系统性重构
- 产品逻辑、状态设计、信息架构、卡片展示、图片能力、H5 体验都已充分逼近 reference
- 已经没有明显待完善空间

## 最终交付物

最终必须给出：
1. reference 深度拆解文档
2. current vs reference 差距分析
3. 重构 PRD
4. 后端改造说明
5. 前端改造说明
6. 实际代码修改结果
7. 最终复盘总结

现在开始执行。

先调用 superpower brainstorm，不允许跳过。

```

扩展设置项
     增加：通知设置、隐私设置、关于我们、版本信息

```
你现在要作为一个“资深产品经理 + 资深 UI/UX 设计师 + 资深全栈工程师（Python backend / Vue + TypeScript H5 frontend）”来工作。

请先扫描整个工程目录，定位资产/总览相关前后端代码，再进入截图分析和方案设计。

你的目标不是简单模仿截图，而是基于参考 App 的截图，深度还原其产品逻辑、信息架构、状态设计、列表组织方式、卡片结构、总览逻辑和展示策略，并将这些能力系统性迁移到当前项目中，对“资产 / 总览”相关模块进行重构，使当前项目在交互和 UI 上尽可能与参考 App 一致。

## 强制执行顺序

1. 必须先调用 superpower 的 brainstorm 能力。
2. 对比分析：
   - docs/images/reference
   - docs/images/current
3. 必须先理解当前工程代码结构，再决定改造方案。
4. 先输出分析结论、差距分析、PRD 和技术改造方案，再开始编码。
5. 每完成一轮后，必须调用 oh-my-claude-code 的 ralph 技能做复盘。
6. 只要相对于 reference 仍有明显差距，就继续迭代，不允许“差不多就结束”。

## 任务目标

你需要完成以下事情：

### 1. 深度分析 reference 截图
对 reference 中每张图都不能只看表面 UI，而要深入分析：

- 页面目标是什么
- 用户进入页面后最先获取什么信息
- 信息架构如何组织
- 总览区、状态区、列表区之间是什么关系
- 是否存在聚合统计、状态切换、筛选、分类、卡片点击、详情跳转等逻辑
- 列表为什么是卡片式而不是普通列表
- 每张卡片承载了哪些主信息 / 副信息 / 状态信息
- 为什么需要图片，图片对识别和体验的价值是什么
- 背后隐含了怎样的数据结构和状态模型

例如看到“总资产统计”“服役中”“已退役”“资产卡片”“资产图片”时，你必须推导出：
- 系统存在资产生命周期状态管理
- 首页承担全局总览职责
- 状态既用于展示，也用于聚合、筛选、分流
- 列表是产品化资产管理，不是简单数据罗列
- 资产展示应尽量支持官方图片 / 品牌图 / 缩略图等更优体验

### 2. 对比 current，找出系统性差距
你必须明确指出当前项目在以下方面与 reference 的差距：

- 总览结构
- 状态管理
- 资产聚合能力
- 列表组织方式
- 卡片化程度
- 图片展示能力
- 筛选/分类能力
- 页面层级和阅读路径
- 交互流畅度
- UI 一致性
- H5 体验质量

并标明这些问题分别属于：
- 产品逻辑问题
- 交互设计问题
- UI 问题
- 前端实现问题
- 后端数据支持问题

## 重要约束：必须适配当前工程

当前工程真实技术栈是：

- 后端：Python
- 前端：Vue + TypeScript
- 端形态：H5

所以你必须严格按这个工程现实来做，避免实现跑偏。

### 后端侧必须重点关注
先理解并基于真实代码判断：
- 资产相关数据模型
- 总览/统计接口
- 状态字段
- 分类字段
- 图片字段
- 聚合逻辑
- 筛选/排序能力

必要时补齐：
- 状态管理字段
- 总览聚合接口
- 列表所需展示字段
- 图片资源字段或映射逻辑
- 分类/筛选支持能力

### 前端侧必须重点关注
先理解并基于真实代码判断：
- Vue 页面结构
- 路由结构
- 资产/总览相关页面
- 组件拆分
- TypeScript 类型定义
- API 调用封装
- 状态管理方式
- 当前 H5 交互方式

必要时重构：
- 首页/总览页结构
- 资产列表区
- 资产卡片组件
- 状态切换组件
- 分类/筛选交互
- 图片展示与兜底
- 加载态/空态/错误态
- H5 点击热区、滚动体验和首屏信息密度

## 不允许出现的错误

- 不要输出 React / Next.js / Flutter / 原生 iOS / Android 方案，除非工程真实存在
- 不要只改样式，不改产品逻辑和数据结构
- 不要把本应后端提供的数据能力硬塞给前端临时拼装
- 不要无视 H5 场景直接照搬原生 App 交互
- 不要想当然重写整个工程，优先在现有代码结构内做可落地重构
- 不要只说建议，必须真正完成代码修改

## 输出要求

在编码前，先输出以下内容：

### A. reference 深度拆解
逐图分析：
- 页面目标
- 信息架构
- 模块结构
- 交互逻辑
- 状态模型
- 列表/卡片逻辑
- 数据需求
- 图片策略

### B. 当前项目差距分析
按模块列出：
- 现状
- 与 reference 的差距
- 问题归属（产品 / UI / 前端 / 后端）
- 改造优先级

### C. 重构 PRD
至少包含：
- 改造目标
- 页面结构重构
- 总览设计
- 状态体系设计
- 资产列表和卡片设计
- 图片展示方案
- H5 交互说明
- 验收标准

### D. 技术改造方案
分别说明：
- Python 后端要改什么
- Vue + TypeScript 前端要改什么
- 需要新增/调整哪些字段、接口、类型、组件
- 如何分阶段落地

## 编码要求

开始实现后，你必须：
1. 先列出改动清单；
2. 按合理顺序推进改造；
3. 保持代码风格与现有工程一致；
4. 避免引入不必要的新依赖；
5. 确保代码可运行，不要只生成草稿；
6. 每完成一轮，都重新对照 reference 检查完成度。

## 强制循环复盘机制

每当你觉得自己做完了，必须立即调用 ralph，从“资深产品经理 + 资深 UI 设计师”的双重视角继续审查：

- 产品逻辑是否真的对齐了 reference
- 首页是否真正形成“总览 -> 状态 -> 列表”的认知路径
- 状态体系是否完整
- 列表是否已经具备卡片化、可识别、可管理能力
- 图片能力是否足够支撑更好的展示体验
- H5 交互是否自然
- UI 是否仍有明显粗糙、不一致、不高级的地方
- 前后端是否仍然存在割裂

每轮复盘都必须输出：
1. 当前完成度评估
2. 相比 reference 的剩余差距
3. 下一轮继续改进项
4. PRD / 设计 / 技术补充

只要还存在明显差距，就继续迭代，直到你确认：
- reference 关键截图已被充分理解
- 当前项目的资产/总览模块已完成系统性重构
- 产品逻辑、状态设计、信息架构、卡片展示、图片能力、H5 体验都已充分逼近 reference
- 已经没有明显待完善空间

## 最终交付物

最终必须给出：
1. reference 深度拆解文档
2. current vs reference 差距分析
3. 重构 PRD
4. 后端改造说明
5. 前端改造说明
6. 实际代码修改结果
7. 最终复盘总结

现在开始执行。

先调用 superpower brainstorm，不允许跳过。

```



```


作为一个“资深产品经理 + 资深 UI/UX 设计师 + 资深全栈工程师“组成的开发团队，你的目标不是简单模仿截图，而是基于参考 App 的截图，深度还原其产品逻辑、信息架构、状态设计、列表组织方式、卡片结构、总览逻辑和展示策略，并将这些能力系统性迁移到当前项目中，对“资产 / 总览”相关模块进行重构，使当前项目在交互和 UI 上尽可能与参考 App 一致。

每当你认为你已经完成后，你需要执行如下步骤：
1. 对比分析：
   - docs/images/reference（参考app的部分截图）
   - 运行chrome来执行仿真测试和检查
3. 每完成一轮后，必须调用 oh-my-claude-code 的 ralph 技能做复盘。
4. 只要相对于 reference 仍有明显差距，就继续迭代，不允许“差不多就结束”。

从“资深产品经理 + 产品受众用户”的双重视角继续审查：

- 产品逻辑是否真的对齐了 reference
- 首页是否真正形成“总览 -> 状态 -> 列表”的认知路径
- 状态体系是否完整
- 列表是否已经具备卡片化、可识别、可管理能力
- 图片能力是否足够支撑更好的展示体验
- H5 交互是否自然
- UI 是否仍有明显粗糙、不一致、不高级的地方
- 前后端是否仍然存在割裂

每轮复盘都必须输出：
1. 当前完成度评估
2. 相比 reference 的剩余差距
3. 下一轮继续改进项
4. PRD / 设计 / 技术补充

只要还存在明显差距，就继续迭代，而不需要反复征询我的意见，直到你确认：
- reference 关键截图已被充分理解
- 当前项目的资产/总览模块已完成系统性重构
- 产品逻辑、状态设计、信息架构、卡片展示、图片能力、H5 体验都已充分逼近 reference
- 已经没有明显待完善空间

```



对于teamlead：你可以自行决策需求实现的优先级，你的目的是保证最终所有teammates协作结束后，交付出来的是一个符合我们需求的成品，当要决策优先实现哪些功能或者需要人工介入时，只要不是对当前系统高危的变更动作，你可以自行判断决定，无需征询我的意见，但是每次实现完一部分功能后，记得压缩一下会话，避免上下文爆炸

对于测试teammate，补充如下 要求：

1、必须使用chrome仿真测试

2、仿真测试必须根据 @docs/images/references/ 的参考截图功能来验证，以排序功能为例，我发现实现的做法是添加时间（正序）、添加时间（倒序），并且单选框选择的方式，但是截图的，是从下
往上的浮窗，并且添加时间这类排序属性放在一块，正序、倒序放在一块，总之，你应该对照references的截图，在测试到具体的页面时，进行实现和截图的比对，推动最终交付的内容与截图一致



所谓的”MR专栏“是资产自定义的一种分类描述，在资产列表往上滑动至截图的资产摘要消失时显示，点击可以筛选对应分类
     下的所有资产（与状态栏起的作用类似，状态栏长期展示，分类栏在下滑至指定位置触发），并且展示样式为使用带光晕效果的下划线表示选中


需要支持多国币种，

一、设置：默认币种，默认为CNY（人民币）,

当点击选择时，展示币种列表，并默认选中已设置的默认币种，并展示为：国旗标识 币种名称（币种代码） 币种符号，如： 🇨🇳 人民币（CNY）¥

列表中将如下主要国家/地区及其货币代码作为收藏放在最前面，且允许用户搜索币种名称、或者代码
中国： 人民币 (CNY)
美国： 美元 (USD)
欧元区： 欧元 (EUR)
日本： 日元 (JPY)
英国： 英镑 (GBP)
澳大利亚： 澳元 (AUD)
加拿大： 加元 (CAD)
瑞士： 瑞士法郎 (CHF)
香港： 港币 (HKD)
新加坡： 新加坡元 (SGD)
俄罗斯： 卢布 (RUB)
印度： 卢比 (INR)
巴西： 巴西雷亚尔 (BRL) 

二、展示
总览页以及心愿单页面，涉及到金额的地方，统一使用币种符号+金额，如：¥10000.00，并根据汇率进行换算，涉及到汇率的地方，需要展示小感叹号图标，点击浮出：录入的币种符号+金额（汇率），如：$10000.00（1 USD = 7 CNY，汇率更新时间：2026-03-24 08:00:00），汇率内容从保存到本地db的汇率中查询（需要缓存避免每次都查db，只有在缓存没有命中才查db）

三、录入
涉及到金额录入的表单位置，如资产录入，统一使用国旗标识 币种符号 + 金额，如：🇨🇳 ¥10000.00，并且允许用户选择币种（选择列表与设置中的币种列表一致）

四、汇率
汇率的处理通过查询开放的api接口`https://api.exchangerate-api.com/v4/latest/CNY`获取，CNY为币种代码(默认以此为基准，汇率为1），返回的json中，rates就是汇率，如：
```json
{
    "provider": "https://www.exchangerate-api.com",
    "WARNING_UPGRADE_TO_V6": "https://www.exchangerate-api.com/docs/free",
    "terms": "https://www.exchangerate-api.com/terms",
    "base": "CNY",
    "date": "2026-03-23",
    "time_last_updated": 1774224001,
    "rates": {
        "CNY": 1,
        "AED": 0.532,
        "AFN": 9.12
    }
}
```
汇率每天从8点-23点，每两个小时更新一次，更新时间需要添加随机时段，避免过于集中到某个时间点