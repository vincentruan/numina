---
date: 2026-04-10
topic: ai-disposal-advisor
---

# 低效资产处置建议（清仓清单）

## Problem Frame

现有 Dashboard 的"低使用率资产"端点已识别 `usage_frequency == 'rarely'` 或 `'idle'` 的资产，但用户面对这个列表无从行动——下一步不明确，"把相机卖掉"是用户已知的建议，真正缺失的是：**量化不行动的成本**、**具体出售渠道**、**保留还是出售的理由**。

本功能在现有低使用率检测之上增加 AI 处置建议层，将被动列表转化为优先级"清仓清单"，帮助家庭释放闲置资产的沉没成本。

**依赖关系：** 此功能依赖创意 #2（智能资产录入助手）提升 `usage_frequency` 字段填写率；字段填写率低时覆盖面窄，但对已填写字段的资产立即生效。

```
每周定时任务（与创意 #3 老化预警统一调度）
         │
         ▼
  查询所有 ai_enabled == True 的家庭
         │
         ▼
  对每个家庭：get_low_usage_assets()（usage_frequency in ['rarely','idle']）
         │
         ▼
  有闲置资产？ ──否──▶ 跳过
         │
         是
         ▼
  对每项资产计算多维度评分（规则，不调用 LLM）
  ├── 闲置时长得分
  ├── 日均成本拖累得分
  ├── 类别情感权重
  └── 当前价值权重
         │
         ▼
  输出处置建议档位（建议出售 / 可考虑出售 / 建议保留）
         │
         ▼
  调用 LLM 生成每项资产的处置建议文本（含渠道建议）
         │
         ▼
  写入 ai_disposal_suggestions 表（UPSERT by asset_id）
         │
         ▼
  用户进入 Dashboard → 摘要卡片 → AIDisposalPage → 清仓清单
```

---

## Requirements

**AI 基础设施前提（依赖 Phase 0）**

- R0. 依赖 `docs/brainstorms/2026-04-10-001-ai-health-report-requirements.md` 中 R0-R6 的基础设施。
- R0a. 定时任务与创意 #3（`ai_asset_alerts`）统一在每周一 08:00 Asia/Shanghai 的调度任务中执行，共享同一个 scheduler job，串行处理老化预警和处置建议两类扫描。

**后端：定时生成**

- R1. 新增 `ai_disposal_suggestions` 表，字段：`id`、`family_id`、`asset_id`（FK，`ON DELETE CASCADE`）、`disposal_verdict`（`sell` / `consider_sell` / `keep`）、`disposal_score`（0-100，规则计算）、`suggestion_text`（AI 生成的中文建议，含渠道）、`estimated_recovery_value`（估算回收价值，基于 `current_value` 打折）、`idle_cost_to_date`（截至生成时的累计闲置成本）、`generated_at`、`is_dismissed`（用户已忽略）。
- R2. 触发条件：`usage_frequency in ['rarely', 'idle']` 且 `status == 'in_use'` 且 `is_archived == False`。
- R3. 多维度评分规则（满分 100，分越高越倾向出售）：
  - **闲置时长**（30分）：`idle` = 30分，`rarely` = 15分
  - **日均成本拖累**（30分）：日均成本 > ¥10 = 30分，¥5-10 = 20分，¥1-5 = 10分，< ¥1 = 0分（日均成本字段已由现有 `compute_daily_cost()` 计算）
  - **类别情感权重**（20分，倾向出售加分）：`数码`/`家电` = 20分，`服饰`/`箱包` = 15分，`运动`/`玩具` = 10分，`家具` = 5分，`珠宝`/`乐器`/`宠物` = 0分（倾向保留）
  - **当前价值**（20分）：> ¥5000 = 20分（高价值值得认真决策），¥1000-5000 = 15分，¥500-1000 = 10分，< ¥500 = 5分
  - **处置档位**：score ≥ 70 = `sell`，40-69 = `consider_sell`，< 40 = `keep`
- R4. `estimated_recovery_value` 计算：`current_value * 折扣率`，折扣率按类别：`数码`/`家电` = 40%，`服饰`/`箱包` = 30%，`珠宝` = 70%，`乐器` = 60%，其他 = 50%。`current_value` 为 NULL 时 `estimated_recovery_value` 为 NULL。
- R5. `idle_cost_to_date`：若有 `annual_maintenance_cost`，计算自 `purchase_date` 至今的累计维护成本；无维护成本则为 NULL。
- R6. 脱敏要求：发送给 LLM 的 prompt 包含资产类别名、`usage_frequency`、`disposal_verdict`（规则计算结果）、`estimated_recovery_value`（金额区间）、`idle_cost_to_date`（金额区间）；不包含资产名称、精确金额、成员信息。
- R7. LLM 生成的建议文本（50-100字）包含：处置建议理由 + 推荐出售渠道（按类别：`数码`/`家电` → 闲鱼/转转，`服饰`/`箱包` → 闲鱼/得物，`珠宝`/`乐器` → 专业回收平台/拍卖，`家具` → 闲鱼/同城，其他 → 闲鱼）；`keep` 档位的建议文本说明保留理由，不给出售渠道。
- R8. UPSERT by `asset_id`，覆盖旧记录并重置 `is_dismissed = False`（与创意 #3 一致）。
- R9. `GET /api/v1/ai/disposal-suggestions` 返回当前家庭所有未忽略的处置建议，按 `disposal_verdict`（sell → consider_sell → keep）再按 `disposal_score` 降序排列。
- R10. `POST /api/v1/ai/disposal-suggestions/{id}/dismiss` 标记 `is_dismissed = True`；下次定时任务 UPSERT 后重置。

**前端：Dashboard 摘要卡片**

- R11. `DashboardPage.vue` 新增"闲置资产"摘要卡片，仅当 `ai_enabled == true` 且有未忽略的处置建议时展示。
- R12. 摘要卡片展示：闲置资产数量、`sell` 档位资产数量、估算总回收价值（所有 `sell` 档位资产的 `estimated_recovery_value` 之和）、"查看清仓清单"按钮。示例："3 项闲置资产，建议出售 2 项，预计可回收约 ¥3,200"。
- R13. 估算回收价值有 NULL 时显示"部分资产价值待更新"提示。

**前端：清仓清单页面**

- R14. 新增 `AIDisposalPage.vue`，从 Dashboard 摘要卡片进入，也可从 `AssetListPage.vue` 筛选入口进入。
- R15. 页面顶部展示汇总数据：闲置资产总数、建议出售数、估算总回收价值、累计闲置成本（所有资产 `idle_cost_to_date` 之和）。
- R16. 资产列表按处置档位分组展示（"建议出售" / "可考虑出售" / "建议保留"三个分组），每组可折叠。
- R17. 每项资产卡片展示：类别 icon、类别名、`usage_frequency` 中文标签（`idle`=闲置、`rarely`=很少使用）、估算回收价值、累计闲置成本（有则展示）、AI 建议文本（最多3行，超出展开）、推荐出售渠道 Chip。
- R18. 点击资产卡片跳转至 `AssetDetailPage.vue`。
- R19. 每项资产卡片右上角有"忽略"按钮，点击后调用 dismiss 接口，该卡片从列表移除（slide-out 动画）；忽略后 Dashboard 摘要数据同步更新。
- R20. 当所有建议均被忽略或无闲置资产时，页面展示空状态："暂无闲置资产建议，继续保持！"。
- R21. 当 `ai_enabled == false` 时，Dashboard 摘要卡片不渲染；`AIDisposalPage.vue` 路由守卫重定向至 Dashboard。

---

## Success Criteria

- 有闲置资产的家庭能在 Dashboard 看到摘要卡片，点击进入完整清单。
- 评分规则可通过手工验算验证（纯规则，无 LLM 参与）。
- 建议文本包含具体出售渠道（可人工抽样验证 10 条）。
- 忽略操作后卡片消失，Dashboard 摘要数据同步更新。
- 资产删除后对应建议自动级联删除（`ON DELETE CASCADE`）。
- `ai_enabled == false` 时 Dashboard 摘要卡片不渲染。

---

## Scope Boundaries

- 不包含出售价格的实时市场查询（估算回收价值基于固定折扣率，非实时行情）。
- 不包含出售流程的跳转链接（不跳转到闲鱼/转转等平台）。
- 不包含"已出售"状态的追踪——用户出售后应通过现有 `AssetSellPage.vue` 流程归档资产，处置建议随资产归档自动级联删除。
- `keep` 档位的资产仍展示在清单中（"建议保留"分组），不过滤掉，让用户有完整视图。
- 不包含批量忽略或批量出售操作（首版逐项操作）。
- 金融资产（`asset_type == 'financial'`）不纳入处置建议扫描——金融资产的处置逻辑与实物资产不同，不适用本功能。

---

## Key Decisions

- **多维度评分（规则）+ LLM 叙事** — 评分逻辑确定性强、可解释、可验证；LLM 只负责将评分结果转化为可读建议文本和渠道推荐。
- **与创意 #3 共享定时任务** — 减少 scheduler job 数量，统一运维；两类扫描串行执行，互不干扰。
- **UPSERT + is_dismissed 重置** — 与创意 #3 保持一致的生命周期管理模式。
- **金融资产排除** — 金融资产的"处置"涉及赎回、卖出等复杂操作，不适用闲置资产的简单处置逻辑，避免误导。
- **`keep` 档位保留在清单中** — 用户需要完整视图；隐藏 `keep` 资产会让用户误以为功能覆盖不全。

---

## Dependencies / Assumptions

- 依赖 Phase 0 基础设施（`agent/` 模块、`ai_enabled` 开关、APScheduler）。
- 依赖创意 #2（智能资产录入助手）提升 `usage_frequency` 字段填写率，但对已填写字段立即生效。
- 依赖创意 #3 的定时任务框架（共享 scheduler job）。
- `compute_daily_cost()` 已在 `backend/app/services/asset.py` 中实现，可直接复用计算日均成本得分。
- 类别情感权重基于系统预定义的 21 个类别，自定义类别默认权重为 10 分（中性）。
- `estimated_recovery_value` 折扣率为固定值，首版硬编码，后续可配置化。
- 资产归档（`is_archived = True`）或状态变更为非 `in_use` 后，下次定时任务不再为其生成建议；`ON DELETE CASCADE` 保证资产删除时建议自动清理。

---

## Outstanding Questions

### Resolve Before Planning

无阻塞问题。

### Deferred to Planning

- **[影响 R3][技术]** 日均成本得分的 ¥10/¥5/¥1 阈值是否合理？首版硬编码，规划时可通过测试数据验证。
- **[影响 R4][技术]** 估算回收价值折扣率按类别硬编码，规划时确认类别 ID 与折扣率的映射表实现方式（dict 常量 vs 数据库配置）。
- **[影响 R0a][技术]** 与创意 #3 共享 scheduler job 时，两类扫描的执行顺序和错误隔离：一类扫描失败不应影响另一类，需要 try/except 分别包裹。
- **[影响 R14][技术]** `AIDisposalPage.vue` 从 `AssetListPage.vue` 的筛选入口进入的具体实现：新增筛选 Tab 还是独立按钮？

---

## Next Steps

→ `/ce:brainstorm`（创意 #6：自然语言资产问答助手）
