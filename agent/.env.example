# Numina Agent 配置模板
# 复制此文件为 .env 并根据环境修改配置

# =============================================================================
# 基础配置
# =============================================================================

# 运行环境: development / production
ENVIRONMENT=development

# =============================================================================
# Backend 通信配置
# =============================================================================

# Backend 服务内部地址（用于 service-to-service 通信）
BACKEND_BASE_URL=http://backend:8000

# Agent 内部通信令牌（必填，与 backend 共享）
# 用于验证 agent ↔ backend 的服务间请求
AGENT_INTERNAL_TOKEN=

# =============================================================================
# 加密配置
# =============================================================================

# AI 加密密钥（与 backend 共享同一个 Fernet key）
# 用于解密存储在 backend 的 AI API Key
# 生成方式: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AI_ENCRYPTION_KEY=

# =============================================================================
# 日志配置
# =============================================================================

# 日志级别: DEBUG / INFO / WARNING / ERROR / CRITICAL
LOG_LEVEL=INFO

# =============================================================================
# DeerFlow 配置
# =============================================================================

# 启用 DeerFlow 集成（默认关闭，迁移验证后开启）
# false: 使用 fallback_engine（直接 LLM 调用）
# true: 使用 deerflow_adapter（通过 DeerFlow 路由）
USE_DEERFLOW=false