// Shared SVG icons — imported as URLs via Vite's ?url suffix.
// Each import produces a hashed asset URL; only referenced icons
// are included in the build output.
//
// Usage:
//   import { homeIcon } from '@numina/assets/icons'
//   <img :src="homeIcon" />
