// Shared raster images (PNG, JPG, WebP, etc.) — imported as URLs.
// Only referenced images are included in the build output.
//
// Usage:
//   import { heroImg } from '@numina/assets/images'
//   <img :src="heroImg" />
