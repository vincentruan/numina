# ═══════════════════════════════════════════════════════════
# 场景: 本地开发 + 本地 PostgreSQL
# 前提: PostgreSQL 运行在 localhost:5432 (brew services / Docker / 其他)
# 用法: cp .env.examples/.env.dev-pgsql-local .env && cp .env.examples/.env.dev-pgsql-local server/.env
# 启动: make dev-all
# ═══════════════════════════════════════════════════════════

ENVIRONMENT=development

# 数据库 — 本地 PostgreSQL (localhost)
DATABASE_URL=postgresql://numina:numinapass@localhost:5432/numina

# DeerFlow checkpoint DB — 本地 PostgreSQL
DEERFLOW_DB_URL=postgresql://numina:numinapass@localhost:5432/numina_deerflow

# CORS
CORS_ORIGINS=["http://localhost:5173","http://localhost:5174","http://localhost:8080"]

# 安全密钥 (务必替换)
SECRET_KEY=CHANGE_ME_SECRET_KEY
ALTCHA_HMAC_KEY=CHANGE_ME_ALTCHA_KEY
AI_ENCRYPTION_KEY=CHANGE_ME_FERNET_KEY
STORAGE_ENCRYPTION_KEY=CHANGE_ME_FERNET_KEY_2

# 服务地址
AGENT_BASE_URL=http://localhost:8001
BACKEND_BASE_URL=http://localhost:8000

# Token
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# Agent
AGENT_INTERNAL_TOKEN=local-test-agent-token-dev
DEERFLOW_DB_URL=postgresql://numina:numinapass@localhost:5432/numina_deerflow

# PostgreSQL 凭据 (用于 make setup-db-postgres)
POSTGRES_DB=numina
POSTGRES_USER=numina
POSTGRES_PASSWORD=numinapass
